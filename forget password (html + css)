<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Forgot Password - EduGestion</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    *, *::before, *::after {
      box-sizing: border-box;
    }
    html, body {
      margin: 0;
      padding: 0;
      font-family: 'Inter', sans-serif;
      background: #ffffff;
      color: #111827;
      height: 100%;
      width: 100%;
    }

    body {
      display: flex;
      align-items: stretch;
      justify-content: center;
    }

    main {
      display: grid;
      grid-template-columns: 1fr 1fr;
      width: 100%;
      max-width: 100%;
      height: 100vh;
    }

    @media (max-width: 767px) {
      main {
        grid-template-columns: 1fr;
        height: auto;
      }
    }

    .left-panel {
      position: relative;
      background:
        linear-gradient(to top, #2563eb 0%, transparent 100%) 0 100% / 100% 50% no-repeat,
        url(blog-20-student-engagement-strategies-captivating-classroom.webp) center center / cover no-repeat;
      color: #f9fafb;
      padding: 48px 32px;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      align-items: center;
      text-align: center;
      gap: 48px;
    }

    .left-panel::after {
      content: "";
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 40%;
      background: linear-gradient(to top, rgba(37, 99, 235, 0.9), rgba(37, 99, 235, 0));
      z-index: 0;
    }

    .left-panel > * {
      position: relative;
      z-index: 1;
    }

    .left-panel h2 {
      font-weight: 700;
      font-size: 1.875rem;
      line-height: 1.3;
      margin: 0;
      max-width: 400px;
    }

    .right-panel {
      background-color: #ffffff;
      padding: 48px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      width: 100%;
      max-width: 100%;
    }

    @media (max-width: 767px) {
      .right-panel {
        padding: 32px 16px;
      }
    }

    .logo-container-left {
  position: absolute;
  top: -25px;
  left: 2px;
  z-index: 10;
  display: flex;
  align-items: center;
}

.logo-container-left img {
  height: auto;
  max-height: auto;      /* Bigger height */
  width: 500px;
  max-width: 150px;      /* Controls width scaling */
  object-fit: contain;
  opacity: 1;
}

    .signin-header {
      text-align: left;
      margin-bottom: 32px;
    }

    .signin-heading {
      font-size: 2rem;
      font-weight: 700;
      color: #111827;
      margin: 0;
    }

    .signin-subtitle {
      font-size: 0.875rem;
      color: #6b7280;
      margin-top: 8px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    label {
      font-size: 0.875rem;
      font-weight: 600;
      color: #374151;
      margin-bottom: 8px;
    }

    input[type="email"] {
      padding: 12px 16px;
      font-size: 1rem;
      border: 1.5px solid #d1d5db;
      border-radius: 8px;
      transition: border-color 0.3s ease;
      width: 100%;
    }

    input:focus {
      border-color: #2563eb;
      outline: none;
      box-shadow: 0 0 8px rgb(37 99 235 / 0.4);
    }

    button.submit-btn {
      background-color: #2563eb;
      color: white;
      font-weight: 700;
      padding: 14px;
      font-size: 1.125rem;
      border-radius: 8px;
      border: none;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.1s ease;
      margin-top: 12px;
    }

    button.submit-btn:hover {
      background-color: #1e40af;
    }

    .back-login {
      text-align: center;
      margin-top: 36px;
      font-size: 0.875rem;
      color: #4b5563;
    }

    .back-login a {
      font-weight: 600;
      color: #2563eb;
    }
  </style>
</head>
<body>
  <main>
    <section class="left-panel">
      <div class="logo-container-left">
        <img src="EduGestion-removebg-preview.png" alt="EduGestion Logo">
      </div>
      <h2>Reset your password and get back on track</h2>
    </section>

    <section class="right-panel">
      <div class="signin-header">
        <h1 class="signin-heading">Forgot Password</h1>
        <p class="signin-subtitle">Enter your registered email and we'll send you reset instructions</p>
      </div>

      <form action="#" method="post">
        <label for="email">Email</label>
        <input id="email" type="email" name="email" placeholder="Enter your email" required />
        <button type="submit" class="submit-btn">Send Reset Link</button>
      </form>

      <p class="back-login">
        Remember your password? <a href="login.html">Back to Sign In</a>
      </p>
    </section>
  </main>
</body>
</html>
