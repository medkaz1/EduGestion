<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Smart Campus Login</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    @media (max-width: 767px) {
  .logo-container-left img {
    height: 40px;
    max-width: 140px;
  }
}

    /* General Reset and Body Styling */
    *, *::before, *::after {
      box-sizing: border-box;
    }
    html, body {
      margin: 0;
      padding: 0;
      font-family: 'Inter', sans-serif;
      background: #ffffff;
      color: #111827;
      height: 100%;
      width: 100%;
    }

    body {
      display: flex;
      justify-content: center;
      align-items: stretch;
    }

    main {
      display: grid;
      grid-template-columns: 1fr 1fr;
      width: 100%;
      height: 100vh;
      max-width: 100%;
    }

    @media (max-width: 767px) {
      main {
        grid-template-columns: 1fr;
        height: auto;
      }
    }

    /* Left Panel */
    .left-panel {
      position: relative;
      background:
        linear-gradient(to top, #2563eb 0%, transparent 100%) 0 100% / 100% 50% no-repeat,
        url(blog-20-student-engagement-strategies-captivating-classroom.webp) center center / cover no-repeat;
      color: #f9fafb;
      padding: 48px 32px;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      align-items: center;
      text-align: center;
      gap: 48px;
    }

    .left-panel::after {
      content: "";
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 40%;
      background: linear-gradient(to top, rgba(37, 99, 235, 0.9), rgba(37, 99, 235, 0));
      z-index: 0;
    }

    .left-panel > * {
      position: relative;
      z-index: 1;
    }

    .left-panel h2 {
      font-weight: 700;
      font-size: 1.875rem;
      line-height: 1.3;
      margin: 0;
      max-width: 400px;
    }

    /* Right Panel */
    .right-panel {
      background-color: #ffffff;
      padding: 48px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      width: 100%;
      max-width: 100%;
    }

    @media (max-width: 767px) {
      .right-panel {
        padding: 32px 16px;
      }
    }

    /* Logo Containers */
    .logo-container-left {
  position: absolute;
  top: -25px;
  left: 2px;
  z-index: 10;
  display: flex;
  align-items: center;
}

.logo-container-left img {
  height: auto;
  max-height: auto;      /* Bigger height */
  width: 500px;
  max-width: 150px;      /* Controls width scaling */
  object-fit: contain;
  opacity: 1;
}



    .logo-text {
      font-weight: 700;
      font-size: 1.75rem;
      color: #111827;
    }

    .logo-subtitle {
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: #2563eb;
    }

    /* Form Styles */
    form {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    label {
      font-size: 0.875rem;
      font-weight: 600;
      color: #374151;
      margin-bottom: 8px;
    }

    input[type="email"],
    input[type="password"],
    select {
      padding: 12px 16px;
      font-size: 1rem;
      border: 1.5px solid #d1d5db;
      border-radius: 8px;
      transition: border-color 0.3s ease;
      width: 100%;
      appearance: none;
      background-color: #fff;
    }

    input:focus,
    select:focus {
      border-color: #2563eb;
      outline: none;
      box-shadow: 0 0 8px rgb(37 99 235 / 0.4);
    }

    select {
      color: #111827;
    }

    button.sign-in {
      background-color: #2563eb;
      color: white;
      font-weight: 700;
      padding: 14px;
      font-size: 1.125rem;
      border-radius: 8px;
      border: none;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.1s ease;
    }

    button.sign-in:hover {
      background-color: #1e40af;
    }

    .form-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 0.875rem;
      color: #4b5563;
    }

    .signup-text {
      text-align: center;
      margin-top: 36px;
      font-size: 0.875rem;
      color: #4b5563;
    }

    .signup-text a {
      font-weight: 600;
      color: #2563eb;
    }

    .signin-header {
      text-align: left;
      margin-bottom: 32px;
    }

    .signin-heading {
      font-size: 2rem;
      font-weight: 700;
      color: #111827;
      margin: 0;
    }

    .signin-subtitle {
      font-size: 0.875rem;
      color: #6b7280;
      margin-top: 8px;
    }
    .role-choices {
  display: flex;
  gap: 16px;
  margin-bottom: 10px;
}

.role-option {
  flex: 1;
  text-align: center;
  padding: 12px;
  font-weight: 600;
  border: 2px solid #d1d5db;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.3s ease;
  user-select: none;
  background-color: #f9fafb;
}

.role-option:hover {
  border-color: #2563eb;
}

.role-option.selected {
  background-color: #2563eb;
  color: white;
  border-color: #2563eb;
}

  </style>
</head>
<body>
  <main>
    <section class="left-panel">
      <div class="logo-container-left">
  <img src="EduGestion-removebg-preview.png" alt="EduGestion Logo">
</div>
      <h2>Stay close to your school, wherever you are</h2>
    </section>

    <section class="right-panel">
      <div class="signin-header">
        <h1 class="signin-heading">Sign In</h1>
        <p class="signin-subtitle">Enter your email and password to login to your dashboard</p>
      </div>

      <form action="#" method="post">
        <label for="email">Email</label>
        <input id="email" type="email" name="email" placeholder="Enter your email" required />

        <label for="password">Password</label>
        <input id="password" type="password" name="password" placeholder="Enter your password" required />

        
        <label for="role">Register as</label>
<div class="role-choices">
  <input type="hidden" name="role" id="role" required />
  <div class="role-option" data-value="student">🎓 Student</div>
  <div class="role-option" data-value="teacher">👨‍🏫 Teacher</div>
</div>


        <button type="submit" class="sign-in">Sign in</button>

        <div class="form-footer">
          <label><input type="checkbox" checked /> Remember me</label>
          <a href="#">Forgot password?</a>
        </div>
      </form>

      <p class="signup-text">
        Don't have an account yet? <a href="#">Join EduGestion</a>
      </p>
    </section>
  </main>
  <script>
  const roleOptions = document.querySelectorAll('.role-option');
  const roleInput = document.getElementById('role');

  roleOptions.forEach(option => {
    option.addEventListener('click', () => {
      roleOptions.forEach(opt => opt.classList.remove('selected'));
      option.classList.add('selected');
      roleInput.value = option.dataset.value;
    });
  });
</script>

</body>
</html>
